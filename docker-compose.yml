services:
  db:
    image: mysql:8.0
    container_name: citas_medicas_db
    restart: always
    environment:
      MYSQL_DATABASE: citas_medicas
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data: